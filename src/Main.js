
import { createButtons, displayButtons, createColorButton } from '../src/myFunctions.js';
// Create buttons using imported function
const arrButtons = createButtons();

// Display the buttons sequentially
displayButtons(arrButtons);

// Create and display a ColorButton
createColorButton();
